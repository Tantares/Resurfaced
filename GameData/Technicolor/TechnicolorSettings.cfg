TECHNICOLOR_SETTINGS
{
  // Emit loading-related debug messages
  DebugLoading = false
  // Emit settings-related debug messages
  DebugSettings = false
  // Emit UI debug messages
  DebugUI = false
  // Emit Editor debug messages
  DebugEditor = false

  // if everything fails, we'll fall back to these swatches
  DefaultPrimarySwatch = porkjetWhite
  DefaultSecondarySwatch = porkjetBlack

  // Keycodes for modes
  PaintModeKey = Alpha6
  SampleModeKey = Alpha7
  FillModeKey = Alpha8
  TogglePaletteKey = Alpha9

  // Rate of scroll in the swatch library
  SwatchLibraryScrollRate = 20
  // Don't change this unless you know what you're doing
  SwatchRenderResolution = 64
}

